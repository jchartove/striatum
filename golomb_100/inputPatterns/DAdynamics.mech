% Mechanism for time-varying DA concentration.

% Parameters.
lowDA=0
highDA=0.5
lowDAtime=5950
highDAtime=6050

% Functions.
mergeDA(t)=(t>lowDAtime)*(t<highDAtime)*((t-lowDAtime)*(highDA-lowDA)/(highDAtime-lowDAtime)+lowDA)
DA(t)=lowDA*(t<=lowDAtime)+highDA*(t>=highDAtime)+mergeDA(t)

% Linkers.
@da = DA(t)